import React from 'react';
import Container from '../components/container'

const Inventory: React.FC = () => {
    return (
        <React.Fragment>
           <h1>Sellers inventory app</h1>
           <Container />
        </React.Fragment>
    );
};

export default Inventory;